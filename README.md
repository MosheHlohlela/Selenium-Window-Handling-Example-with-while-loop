# Selenium-Window-Handling-Example-with-while-loop
Count links on a webpage.
Limit WebDriver scope to specific sections.
Open multiple links in new tabs.
Handle multiple browser windows and print their titles.

